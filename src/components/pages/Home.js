import React, { Component } from 'react'
import GoogleMap from './../components/GoogleMap'

const Home = props => (
  <div>
    This is the Home page.
    <GoogleMap />
  </div>
);

export default Home